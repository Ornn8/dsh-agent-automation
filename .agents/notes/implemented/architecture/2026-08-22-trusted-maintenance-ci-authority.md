Maintenance promotion now treats ordinary Controller CI as functional evidence only after trusted code verifies the fixed `.github/workflows/controller-ci.yml` path, Controller repository, `pull_request` event, exact PR number/base/head pair, completed successful workflow run, and an Actions CheckRun bound to that run; a candidate change to the fixed workflow fails closed and remains manual.

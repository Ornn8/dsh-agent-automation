# PR5 capacity-wait foundation

This slice adds a strict, sanitized `CapacityWaitProjection v1`, a pure resume identity, and a pure selector for current open Issue or pull-request subjects. It intentionally has no projection writer, scheduler, workflow dispatch, review or repair integration, durable service, journal, receipt, or runtime activation.

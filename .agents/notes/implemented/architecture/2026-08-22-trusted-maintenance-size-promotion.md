Trusted Controller maintenance promotion now rereads the open repair pull request before `gh pr merge`, measures its GitHub file additions and deletions with the Controller's `pull-request-size` policy, and supplies the current body for split-rationale validation. It rejects a failed policy decision and confirms the same head and body immediately before merge; ordinary Controller CI remains functional evidence rather than the size authority.

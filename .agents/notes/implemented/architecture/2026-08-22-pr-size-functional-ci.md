# Pull request size evidence

The ordinary `pull_request` Controller CI scope job reports measured file and changed-line counts against the default review target and enforces a visible level-two `Split rationale` section for above-target changes within the absolute caps. GitHub event bodies are input to this advisory functional check; the candidate-owned workflow is not independent merge authority against malicious same-repository workflow changes.

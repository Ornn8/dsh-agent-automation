# Worker verification receipt observation

Configured Issue and transported repair Workers now persist their already-bound v2 Verification Contract receipt inside the existing authenticated status comment. Exact-head review validates the receipt against the current trusted contract and selects current-branch repair evidence before closing-Issue change evidence. The observation is structured review context only; it cannot authorize a verdict, replace CI, or change landing. Unconfigured Profiles and legacy status comments remain unchanged.

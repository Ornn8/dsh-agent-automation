name: Reconcile Exact-Pair Reviews

on:
  workflow_call:
    inputs:
      repository:
        required: true
        type: string
      controller_sha:
        required: true
        type: string

concurrency:
  group: dsh-review-reconcile-${{ inputs.repository }}
  cancel-in-progress: true

jobs:
  reconcile:
    name: codex/reconcile-base
    runs-on: [self-hosted, Windows, X64, dsh-agent]
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - name: Validate immutable controller revision
        env:
          CONTROLLER_SHA: ${{ inputs.controller_sha }}
        run: |
          if ($env:CONTROLLER_SHA -notmatch '^[0-9a-f]{40}$') {
            throw 'controller_sha must be a full commit SHA'
          }

      - name: Check out automation controller
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          repository: Ornn8/dsh-agent-automation
          ref: ${{ inputs.controller_sha }}
          path: controller
          persist-credentials: false

      - name: Reconcile open pull requests after a base update
        env:
          TARGET_REPOSITORY: ${{ inputs.repository }}
        run: node controller/src/reconcile-reviews.mjs
